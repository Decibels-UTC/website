import { Button } from 'semantic-ui-react'


//Composant qui va réagir à la connexion via django.

function LoginButton(){

    return(
  <div>

    <Button color='red'>Login</Button>

  </div>
)}

export default LoginButton